import constants from "@utils/constants";
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  bottomButtons: {
    position: "absolute",
    padding: constants.padding * 3,
    bottom: 0,
    width: "100%",
    justifyContent: "center",
  },
});
